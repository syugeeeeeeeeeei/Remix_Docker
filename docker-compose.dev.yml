services:
  remix:
    build:
      target: dev # 開発ステージをビルド
    container_name: remix_app_dev
    volumes:
      - ./remix:/app
      - /app/node_modules # ホストのnode_modulesをマウントしない
    tty: true
    stdin_open: true


  api:
    build:
      target: dev # 開発ステージをビルド
    container_name: api_server_dev
    volumes:
      - ./api:/app
      - /app/node_modules
    tty: true
    stdin_open: true


  postgres:
    container_name: database_dev
